from setuptools import setup

setup(name='dronecore_mission',
      version='0.2',
      description='DroneCore gRPC client for the mission plugin.',
      url='https://github.com/dronecore/dronecore-python',
      packages=['dronecore_mission'])
