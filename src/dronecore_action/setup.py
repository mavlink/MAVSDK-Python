from setuptools import setup

setup(name='dronecore_action',
      version='0.2',
      description='DroneCore gRPC client for the action plugin.',
      url='https://github.com/dronecore/dronecore-python',
      packages=['dronecore_action'])
