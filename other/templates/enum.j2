class {{ name.upper_camel_case }}(Enum):
    """ Generated by dcsdkgen """

    {% for value in values %}
    {{ value.uppercase }} = {{ loop.index - 1 }}
    {%- endfor %}

    def translate_to_rpc(self, rpc{{ name.upper_camel_case }}):
        rpc{{ name.upper_camel_case }} = {
            {%- for value in values %}
                {{ loop.index - 1 }}: {{ plugin_name.lower_snake_case }}_pb2{% if parent_struct is not none %}.{{ parent_struct.upper_camel_case }}{% endif %}.{{ value.uppercase }}{% if not loop.last %},{% endif %}
            {%- endfor %}
            }.get(self.value, None)

    @staticmethod
    def translate_from_rpc(response):
        """ Parses a gRPC response """
        return {
            {%- for value in values %}
                {{ loop.index - 1 }}: "{{ value.uppercase }}",
            {%- endfor %}
            }.get(response.{{ name.lower_snake_case }}, None)
